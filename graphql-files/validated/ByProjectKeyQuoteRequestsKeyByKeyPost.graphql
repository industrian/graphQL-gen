mutation {
  updateQuoteRequest(
    version: 1
    key: "{key}"
    actions: [{ changeQuoteRequestState: { quoteRequestState: Cancelled } }]
  ) {
    id
    version
    lastModifiedAt
    #...
  }
}
