query {
  asAssociate(
    businessUnitKey: "{businessUnitKey}"
    associateId: "{associateId}"
  ) {
    order(orderNumber: "{orderNumber}") {
      id
      version
      createdAt
      #...
    }
  }
}
