mutation {
  updateShoppingList(
    storeKey: "{storeKey}"
    version: 1
    key: "{key}"
    actions: [{ changeName: { name: [{ locale: "en", value: "New Name" }] } }]
  ) {
    id
    version
    lastModifiedAt
    #...
  }
}
