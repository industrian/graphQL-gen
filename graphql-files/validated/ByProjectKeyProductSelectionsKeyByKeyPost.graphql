mutation {
  updateProductSelection(
    version: 1
    key: "{key}"
    actions: [
      { changeName: { name: [{ locale: "en", value: "new selection name" }] } }
    ]
  ) {
    id
    version
    lastModifiedAt
    #...
  }
}
