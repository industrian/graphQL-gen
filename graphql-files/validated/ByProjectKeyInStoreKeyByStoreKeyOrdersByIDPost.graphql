mutation {
  updateOrder(
    storeKey: "{storeKey}"
    version: 1
    id: "{id}"
    actions: [{ changeOrderState: { orderState: Complete } }]
  ) {
    id
    version
    lastModifiedAt
    #...
  }
}
