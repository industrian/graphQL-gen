mutation {
  updateQuote(
    storeKey: "{storeKey}"
    version: 1
    id: "{id}"
    actions: [{ changeQuoteState: { quoteState: Accepted } }]
  ) {
    id
    version
    lastModifiedAt
    #...
  }
}
