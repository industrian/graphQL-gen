query {
  asAssociate(
    businessUnitKey: "{businessUnitKey}"
    associateId: "{associateId}"
  ) {
    orders {
      offset
      count
      total
      results {
        id
        version
        createdAt
        #...
      }
    }
  }
}
