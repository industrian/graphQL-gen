mutation {
  updateCartDiscount(
    storeKey: "{storeKey}"
    version: 1
    key: "{key}"
    actions: [{ changeValue: { value: { relative: { permyriad: 1000 } } } }]
  ) {
    id
    version
    lastModifiedAt
    #...
  }
}
