query {
  asAssociate(
    businessUnitKey: "{businessUnitKey}"
    associateId: "{associateId}"
  ) {
    carts {
      offset
      count
      total
      results {
        id
        version
        createdAt
        #...
      }
    }
  }
}
