mutation {
  updateShoppingList(
    storeKey: "{storeKey}"
                        version: 1
    id: "{id}"
    actions: [{ changeName: { name: [{ locale: "en", value: "New Name" }] } }]
  ) {
    id
    version
    lastModifiedAt
    #...
  }
}
