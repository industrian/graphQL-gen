mutation {
  updateQuote(
    storeKey: "{storeKey}"
    version: 1
    key: "{key}"
    actions: [{ changeQuoteState: { quoteState: Accepted } }]
  ) {
    id
    version
    lastModifiedAt
    #...
  }
}
